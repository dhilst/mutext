---
title: INC-0001 Crash
description: Interview exercise for μ-text troubleshooting candidates.
permalink: /puzzles/001-tutorial.html
---

{% include lore-block.html chapter="01" label="the interview" %}

After solving 32 logic challenges and inverting 1,024 linked lists, Andy finally reached the last interview step.

The meeting room was empty except for a terminal, a whiteboard, and Bob.

Bob did not look up from his coffee.

"Andy, right?"

Andy nodded.

Bob pointed at the terminal.

"Good. One of our generated systems crashed last night. Three processes were running, three access tokens were active, and three resources were being touched. One process crashed while accessing one resource with an invalid token."

Andy frowned. "So this is debugging?"

Bob smiled.

"No. This is troubleshooting."

He tapped the whiteboard.

"Find which process crashed. Enter it as process, token, resource."

{% include lore-block_end.html %}

{% include puzzle-grid.html %}
  {% include puzzle-col.html %}

    {% include panel.html title="clues" pill="retained" pill_tone="green" markdown=true %}

1. The `parser` was not using `token-alpha`.
2. The process accessing `/cache` was using `token-gamma`.
3. The `crawler` was accessing `/cache`.
4. The process using `token-alpha` was accessing `/logs`.
5. `token-beta` was invalid.
6. The `renderer` was not using `token-beta`.

    {% include panel_end.html %}

    {% include panel.html title="messages" subtitle="bob" raw=true %}
<div class="max-h-80 overflow-y-auto">
  {% include chat-message.html sender="Bob" time="09:05" message="Alright, let's work through this. The table has two halves: the left three columns are tokens, the right three are resources. Each row is a process." %}
  {% include chat-message.html sender="Bob" time="09:06" message="Mark an ✕ when you can rule something out. Mark a ✓ when you're sure of a match. Start with what the clues tell you directly." %}
  {% include chat-message.html sender="Bob" time="09:07" message="One rule: each process holds exactly one token and accesses exactly one resource. No sharing, no duplicates. So when you confirm a match, you can rule out everything else in that row and column." %}
  {% include chat-message.html sender="Bob" time="09:08" message="Clue 1 says parser was NOT using token-alpha. That's an ✕ in the parser row, token-alpha column." %}
  {% include chat-message.html sender="Bob" time="09:08" message="Clue 3 says crawler was accessing /cache. That's a ✓. And if crawler has /cache, the other two processes don't — mark ✕ for parser×/cache and renderer×/cache. Also mark ✕ for crawler×/logs and crawler×/models since crawler already has its resource." %}
  {% include chat-message.html sender="Bob" time="09:09" message="Clue 6 says renderer was NOT using token-beta. Another ✕." %}
  {% include chat-message.html sender="Bob" time="09:10" message="Now is time for deduction! Clue 2 says the process accessing /cache was using token-gamma. Clue 3 says crawler accesses /cache. So crawler uses token-gamma — mark a ✓ in token-gamma×crawler." %}
  {% include chat-message.html sender="Bob" time="09:11" message="Clue 5 says token-beta was invalid — that's the crash token. Crawler has token-gamma, so crawler didn't crash." %}
  {% include chat-message.html sender="Bob" time="09:12" message="Keep eliminating. When a row or column has only one empty cell left, that's your match. Fill it all in and enter your answer below." %}
</div>
    {% include panel_end.html raw=true %}

  {% include puzzle-col_end.html %}

  {% include puzzle-col.html %}

    {% include panel.html title="table" subtitle="operator marks" subtitle_class="text-slate-mutext" class="min-w-0 overflow-hidden" raw=true %}

<div class="border-b border-slate-700/70 bg-slate-950/45 p-4">
  <div class="grid gap-3 md:grid-cols-3">
    <div>
      <p class="font-mono text-xs uppercase text-slate-mutext">fault</p>
      <p class="mt-1 text-sm text-white">Process crash — invalid access token</p>
    </div>
    <div>
      <p class="font-mono text-xs uppercase text-slate-mutext">scenario</p>
      <p class="mt-1 text-sm text-white">eval-01 / interview exercise</p>
    </div>
    <div>
      <p class="font-mono text-xs uppercase text-slate-mutext">crash time</p>
      <p class="mt-1 text-sm text-amber-warn">23:42:44</p>
    </div>
  </div>
</div>

{% include zebra-table.html cols_a="parser|crawler|renderer" cols_b="/cache|/logs|/models" rows_a="token-alpha|token-beta|token-gamma" rows_b="/cache|/logs|/models" %}

    {% include panel_end.html raw=true %}

    {% include panel.html title="answer input" subtitle="incident response" markdown=true %}

**Question:** Which process crashed, which token was it using, and which resource was it accessing?

<div data-puzzle-answer="parser, token-beta, /models">
<label class="block font-mono text-xs uppercase text-slate-mutext mb-3">operator answer</label>
<div class="flex flex-wrap items-center gap-3">
  <select data-answer-dim="0" class="bg-slate-950/70 border border-slate-700/80 text-sm text-slate-100 font-mono px-3 py-2 outline-none focus:border-cyan-trace">
    <option value="">— process —</option>
    <option value="parser">parser</option>
    <option value="crawler">crawler</option>
    <option value="renderer">renderer</option>
  </select>
  <select data-answer-dim="1" class="bg-slate-950/70 border border-slate-700/80 text-sm text-slate-100 font-mono px-3 py-2 outline-none focus:border-cyan-trace">
    <option value="">— token —</option>
    <option value="token-alpha">token-alpha</option>
    <option value="token-beta">token-beta</option>
    <option value="token-gamma">token-gamma</option>
  </select>
  <select data-answer-dim="2" class="bg-slate-950/70 border border-slate-700/80 text-sm text-slate-100 font-mono px-3 py-2 outline-none focus:border-cyan-trace">
    <option value="">— resource —</option>
    <option value="/cache">/cache</option>
    <option value="/logs">/logs</option>
    <option value="/models">/models</option>
  </select>
  <button class="button-primary" type="button" data-fake-action="SUBMIT">SUBMIT</button>
</div>
</div>

    {% include panel_end.html %}

  {% include puzzle-col_end.html %}
{% include puzzle-grid_end.html %}

<section id="lore-reveal" class="mt-5 hidden">
{% include lore-block.html chapter="01" label="after the interview" %}

Bob leaned back and looked at Andy for the first time.

"Parser. Token-beta. /models." He took a sip of coffee. "Most candidates stare at the screen for twenty minutes and then guess. You actually worked the table."

He stood up, pulled a keycard from his shirt pocket, and placed it on the desk.

"Welcome to μ-text. You start Monday. Your desk is on the fourth floor, next to the vending machine that dispenses only error codes. Don't ask."

He paused at the door.

"Oh — and the system that crashed last night? That wasn't a simulation. That was production. You just filed your first real incident."

{% include lore-block_end.html %}

<div class="flex justify-center pt-2">
  <a class="button-primary" href="{{ '/puzzles/002-the-lockup.html' | relative_url }}">NEXT INCIDENT →</a>
</div>
</section>

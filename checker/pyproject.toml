[project]
name = "zebra-checker"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "z3-solver",
    "pyyaml",
]

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [".", "tests"]
addopts = "-q"
